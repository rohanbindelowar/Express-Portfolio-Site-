<!-- FILENAME- views/partials/main_nav.ejs 
rohan(301283416) -->

<!-- top navigation bar for my site -->
<nav class="navbar fixed-top  main-nav">
    <div class="container"></div>

    <!-- when logo is clicked it will be back to home page -->
    <a href="/home">
        <img src="../../public/image/logo.png" alt="logo" width="50px">
    </a>
    <div>
        <ul class="navbar-nav">
            <!-- Link for home page -->
            <li class="nav-item">
                <% if(title=="Home") { %>
                    <a class="nav-link active" aria-current="page" href="home.ejs">
                <% } else { %>
                    <a class="nav-link" aria-current="page" href="home.ejs"
                <% } %>
                <i class="fa-solid fa-house-chimney-window" style="color: #38346f;"></i>
            </li>
            <!-- link for about Me page  -->
            <li class="nav-item">
                <% if(title=="About Me") { %>
                    <a class="nav-link active" aria-current="page" href="about.ejs">
                <% } else { %>
                    <a class="nav-link" aria-current="page" href="about.ejs"
                <% } %>
                <i class="fa-regular fa-circle-info" style="color: #344a6f;"></i>
            </li>

             <!-- link for Projects page  -->
             <li class="nav-item">
                <% if(title=="Projects") { %>
                    <a class="nav-link active" aria-current="page" href="projects.ejs">
                <% } else { %>
                    <a class="nav-link" aria-current="page" href="projects.ejs"
                <% } %>
                <i class="fa-regular fa-diagram-project"></i>
            </li>

            <!-- link for Services page  -->
            <li class="nav-item">
                <% if(title=="Services") { %>
                    <a class="nav-link active" aria-current="page" href="services.ejs">
                <% } else { %>
                    <a class="nav-link" aria-current="page" href="services.ejs"
                <% } %>
                <i class="fa-regular fa-briefcase"></i>
            </li>

             <!-- link for Contact Me page  -->
             <li class="nav-item">
                <% if(title=="Contact Me") { %>
                    <a class="nav-link active" aria-current="page" href="contact_me.ejs">
                <% } else { %>
                    <a class="nav-link" aria-current="page" href="contact_me.ejs"
                <% } %>
                <i class="fa-regular fa-envelope"></i>
            </li>
        </ul>
    </div>


</nav>